"# Iris Species Classifier 
This project uses Machine Learning to classify flower species based on sepal and petal measurements.

## Technologies Used
- Python
- Scikit-Learn
- Pandas

## How to Run
1. Install requirements: `pip install -r requirements.txt`
2. Run the script: `python main.py`"
